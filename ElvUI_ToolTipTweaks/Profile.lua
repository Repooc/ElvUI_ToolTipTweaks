local _, _, _, P = unpack(ElvUI)

P.tooltiptweaks = {
    enable = false,
    healthBar = {
        enable = false,
        spacing = 0,
    },
    padding = {
        ignoreBagFrame = false,
        useDefaultPadding = true,
        xOffset = 0,
        yOffset = 0,
    },
}
